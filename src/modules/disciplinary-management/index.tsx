import { mockCases, mockIncidents } from "../disciplinary-cases/data/mockCases";

export default function DisciplinaryManagement() {
  const resolved = mockIncidents.filter((incident) => incident.outcome === "Resolved Locally").length;
  return <div className="dashboard-page"><div><h1 className="page-title">Disciplinary Management</h1><p className="page-subtitle">Governance overview across supervisor-resolved incidents and HR disciplinary cases.</p></div><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))", gap: 12 }}><Metric label="All Incidents" value={mockIncidents.length} /><Metric label="Resolved Locally" value={resolved} /><Metric label="Escalated to HR" value={mockIncidents.length - resolved} /><Metric label="HR Cases" value={mockCases.length} /></div><div className="panel"><div className="panel-header"><h2 className="panel-title">Incident Audit Register</h2></div><div className="panel-body table-wrap"><table className="table"><thead><tr><th>Incident</th><th>Employee</th><th>Source</th><th>Department</th><th>Outcome</th><th>Linked HR Case</th></tr></thead><tbody>{mockIncidents.map((incident) => <tr key={incident.id}><td>{incident.id}</td><td>{incident.employee}</td><td>{incident.source}</td><td>{incident.department}</td><td>{incident.outcome}</td><td>{incident.escalatedCaseId ?? "—"}</td></tr>)}</tbody></table></div></div></div>;
}
function Metric({ label, value }: { label: string; value: number }) { return <div className="summary-card"><div style={{ color: "var(--text-secondary)", fontSize: ".75rem" }}>{label}</div><strong style={{ fontSize: "1.35rem" }}>{value}</strong></div>; }
